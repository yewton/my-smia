server.port: 8082

spring:
  application:
    name: authentication-service
  profiles:
    active: dev
    include: eureka
  cloud:
    config:
      uri: http://config-server:8071
      fail-fast: true
